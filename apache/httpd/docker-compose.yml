name: mdevoldere-httpd
services:
  apache:
    # image: httpd:2.4
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mdevoldere-httpd-alpine
    restart: unless-stopped
    ports:
    - '8003:80'
    volumes:
    - ./htdocs:/usr/local/apache2/htdocs